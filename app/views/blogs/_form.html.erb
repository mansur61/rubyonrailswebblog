
     
      <%= form_with(model: blog) do |form| %>
      <% if blog.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

        <ul>
          <% blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
      <% end %>

      <div class="field">
        <%= form.label :baslik,class:"form-label" %>
        <%= form.text_field :baslik, class:"form-control"  %>
      </div>

      <div class="field">
        <%= form.label :icerik, class:"form-label" %>
        <%= form.text_field :icerik, class:"form-control" %>
      </div>

      <div class="actions"> 
        <%= form.submit 'Blog Yaz',class:"btn btn-secondary mt-4"%>
      </div>
      <% end %>
